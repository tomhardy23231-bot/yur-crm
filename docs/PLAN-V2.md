# ПЛАН v2 — Подразделения · ЗП-режимы · Акты · Отпуска · Касса

> **Источник правды текущего цикла разработки.** Согласован с пользователем 2026-06-10
> по требованиям клиента (переписка с Ириной Лященко от 09.06.2026 + образец XLS
> «Оборотно-сальдова звітність ОЛІМП» → копия в `docs/samples/`).
>
> Цикл разбит на **7 этапов. Каждый этап = отдельная сессия.** Это сделано намеренно,
> чтобы не переполнять контекст и не допускать галлюцинаций в длинной сессии.

---

## Протокол сессии «ПРОДОЛЖАЙ»

Пользователь начинает сессию словом **«ПРОДОЛЖАЙ»**. Агент обязан:

1. Прочитать `CLAUDE.md` (особенно §4, §5, §7), затем этот файл: **статус-таблицу**
   ниже и раздел своего этапа, затем последнюю запись `docs/PROGRESS.md`.
2. Определить по статус-таблице первый этап со статусом ⬜ — это и есть задача сессии.
   Делать **только его**. Следующий этап в той же сессии НЕ начинать.
3. Перед миграциями/крупными решениями — короткий план пользователю и ждать «ок»
   (правило проекта).
4. **Завершение этапа — обязательный чеклист:**
   - [ ] `npx tsc --noEmit` + `npm run lint` + `npm run build` — зелёные;
   - [ ] миграции применяются на чистую БД: `npx supabase db reset`;
   - [ ] тесты затронутых областей проходят (Vitest; для RLS — integration-тесты видимости);
   - [ ] `/review` диффа; для этапов с RLS/правами (1, 2, 4, 5, 7) — дополнительно `/cso`;
   - [ ] обновить разделы `CLAUDE.md`, указанные в этапе («Доки» в конце этапа);
   - [ ] отметить этап в статус-таблице ниже (✅ + дата + хеш коммита);
   - [ ] добавить handoff-запись в `docs/PROGRESS.md`;
   - [ ] один общий коммит этапа (не дробить); **пуш на прод (master → Vercel) — только
     после явного «ок» пользователя**;
   - [ ] деплой миграций на прод: `npx supabase db push` — тоже только по «ок»,
     и **строго после свежего дампа прода** (раздел «Бэкап и откат» ниже;
     ⚠ помнить гочу allowlist `activity_log`, см. «Подводные камни»).

## Статус этапов

| # | Этап | Статус | Дата | Коммит |
|---|------|--------|------|--------|
| 1 | БД-фундамент: departments, должности, видимость | ✅ | 2026-06-10 | 0e69959 |
| 2 | RLS: новая модель доступа | ✅ | 2026-06-10 | 307b9d3 |
| 3 | UI: подразделения, команда, доступы | ✅ | 2026-06-10 | 7a17e97 |
| 4 | ЗП-режимы: фикс / фикс+% / % | ✅ | 2026-06-10 | e9ee0da |
| 5 | Акты как платёжные документы | ✅ | 2026-06-10 | 915900e |
| 6 | Отпуска / отсутствия | ✅ | 2026-06-10 | 45a38b2 |
| 7 | Касса и сальдо-отчёт | ✅ | 2026-06-11 | _follow-up_ |

---

## Целевая модель (спека, согласована)

### Подразделения и доступ
- Новая сущность `departments` — изначально 10 (Київський, Дніпровський, Львівський,
  Одеський + «Підрозділ 5…10» — названия клиент даст позже). Количество не ограничено.
- Каждый сотрудник привязан к подразделению (`users.department_id`) и имеет
  отображаемую должность (`users.position`, свободный текст: керівник, заступник,
  юрист ВП, менеджер ВП, експерт, адміністратор). Штатка клиента ~20 чел/подразделение,
  лимитов нет.
- **Роли доступа остаются прежние 5** (owner, admin, office_manager, lawyer, expert).
  Маппинг должностей: керівник/заступник → `admin`; юрист ВП/менеджер ВП → `lawyer`;
  експерт → `expert`; адміністратор → `office_manager`.
- Дела пересекают подразделения: дело «принадлежит» подразделению юриста-продажника
  И подразделению эксперта-исполнителя одновременно — его видят оба руководителя.
- **Видимость:**
  - `owner` — всё, всегда (режим бога), не настраивается;
  - `admin`/`office_manager` — `users.visibility_scope`: `'department'` (по умолчанию —
    видит дела/финансы/людей только своего подразделения) или `'all'` (вся компания).
    Выставляет только owner, индивидуально каждому сотруднику (из карточки подразделения);
  - `lawyer`/`expert` — всегда только свои дела (`lawyer_id`/`responsible_id`),
    НЕ настраивается;
  - переходное правило: admin/office_manager с `department_id IS NULL` видит всё
    (поведение как до v2), чтобы система не «ослепла» до раскидывания людей.

### Зарплата — три режима (на сотруднике)
- `users.salary_mode`: `'percent'` (по умолчанию, текущая модель) | `'fixed'` |
  `'fixed_percent'`; `users.salary_fixed_amount` — фикс в месяц.
- Процентная часть — существующая механика без изменений (ставка категории
  `payroll_rates` + override на деле).
- Права назначения: owner — всем; admin — сотрудникам **своего** подразделения.

### Акты = платёжные документы (мини-биллинг)
Документ — **«Рахунок-Акт»** (счёт и акт в одном, образец клиента:
`docs/samples/rahunok-akt-sample.xlsx`). Это же закрывает тему «инвойсов» из Phase 2.

Цикл (уточнён с клиентом 10.06.2026):
1. **СРМ генерирует акт** по образцу: сквозной номер («Рахунок-Акт № N від <дата>»),
   реквизиты исполнителя из настроек компании, заказчик из карточки клиента
   (ФИО/назва + РНОКПП/ЄДРПОУ), услуга и сумма, сумма прописью (укр.),
   призначення платежу, блок «Умови прийняття», подписи.
2. Акт выдаётся клиенту (печать / PDF / отправка) — статус `issued`.
3. Клиент оплачивает.
4. В СРМ **загружают скан с подписями/печатью и прописывают оплаченную сумму** —
   статус `paid`: автоматически создаётся платёж по делу на эту сумму; отметка
   выполнения `full`/`partial` вычисляется по сумме (накопительно оплаченное по
   актам ≥ суммы договора → `full`, иначе `partial`; staff может поправить вручную).
   Скан обязателен — именно подписанный экземпляр считается подтверждением.
5. После подтверждения пересчитываются `paid_total`/долг и **просчитываются выплаты
   (ЗП)** существующим движком от оплат. Предоплаты вносятся платежами напрямую.

- Закрытие дела: **блок**, если нет акта `completion='full'` со статусом `paid`;
  совсем без актов — мягкое предупреждение (как было). ⚠ Допущение — уточняется у
  клиента, см. «Открытые вопросы».
- Эксперт получает право создавать задачи по своим делам (правка §7-6).

### Отпуска
`absences`: тип (отпуск/болезнь/другое), период с…по, комментарий. Вносит сам сотрудник,
admin своего подразделения, owner. Видно в карточке сотрудника и в календаре.

### Касса (по образцу XLS ОЛІМП)
- Счета: Карта / Рахунок / Готівка (+добавляемые), у счёта начальный остаток и дата.
- Журнал операций: приход/расход за день, свободное описание (аренда, налоги, реклама —
  НЕ привязаны к делам); платежи по делам автоматом падают приходом на выбранный счёт.
- Отчёт: разворот по дням (остаток на начало → приходы (до N строк) → расходы →
  остаток на конец), вкладки по счетам + сводный Total; сальдо на день и месяц.
- Доступ: по умолчанию только owner; owner выдаёт право точечно.

### Открытые вопросы (не блокируют этапы 1–4)
1. ~~Точные условия блокировки закрытия дела~~ → **решено на Этапе 5: жёсткого блока
   НЕТ, только мягкое предупреждение** (решение пользователя). `case_acts.completion`
   (full/partial) считается, но закрытие в `closed` не блокируется. Если клиент захочет
   жёсткий блок (нет акта full+paid → нельзя закрыть) — включается одной правкой
   БД-триггера на `cases.stage` + ошибкой в UI. Подтвердить у клиента.
2. Названия остальных 6 подразделений — клиент даст позже (заводим «Підрозділ N»).
3. Порог `full`/`partial`: принято допущение «оплачено по актам ≥ суммы договора →
   full» (+ ручная правка staff). Подтвердить у клиента на этапе 5.

---

## Этап 1 — БД-фундамент: departments, должности, видимость

**Цель:** схема и данные для подразделений, без смены поведения системы (RLS пока старое).

Задачи:
- Миграция: таблица `departments` (`id uuid pk default gen_random_uuid()`,
  `name text not null`, `is_active boolean not null default true`, `created_at`).
- `users`: добавить `department_id uuid null references departments(id)`,
  `position text null`, `visibility_scope text not null default 'department'
  check (visibility_scope in ('department','all'))`.
- RLS на `departments`: SELECT — все аутентифицированные; INSERT/UPDATE/DELETE — owner.
- Сидинг (миграцией или seed): 10 подразделений (4 именованных + Підрозділ 5…10);
  в `scripts/seed.ts` привязать тестовых пользователей к подразделениям и дать должности.
- `activity_log`: если логируем действия по departments — расширить allowlist
  (см. «Подводные камни»).

DoD: `db reset` чистый; tsc/lint/build зелёные; поведение приложения НЕ изменилось
(никакой UI/RLS-логики на новых полях); существующие тесты проходят.
Доки: CLAUDE.md §5 — добавить `departments` и новые поля `users`.

## Этап 2 — RLS: новая модель доступа

**Цель:** переключить видимость с «staff видит всё» на модель подразделений.

Задачи:
- SQL-функции: `current_user_department()`; переписать `can_see_case` /
  `can_write_case` (и места, где используется `is_staff()` для видимости данных):
  - owner → всё;
  - admin/office_manager: `visibility_scope='all'` ИЛИ `department_id IS NULL` → всё;
    иначе → дела, где подразделение `lawyer_id` ИЛИ `responsible_id` совпадает с их;
  - lawyer/expert — без изменений.
- `is_staff()` оставить для операций, не зависящих от подразделения (создание
  клиентов/дел и т.п.); деструктив (`can_manage_users`) и системные настройки
  (`is_owner`) — без изменений.
- Наследуемые таблицы (documents, tasks, payments, comments, activity_log по делу)
  автоматически следуют новому `can_see_case` — проверить каждую политику.
- Отчёты (payroll и пр.): admin видит людей/цифры своего подразделения.
- Integration-тесты видимости: матрица «руководитель Киева видит дело Киев-продал/
  Днепр-исполняет; руководитель Львова — НЕ видит; scope='all' видит всё; NULL
  department видит всё; lawyer/expert — без изменений».

DoD: тесты матрицы зелёные; `/cso` + `/review` пройдены.
Доки: CLAUDE.md §4 (матрица доступа), §7-3, §7-5.

> **✅ Закрыт 2026-06-10.** Миграция `20260610110000_department_scope_rls.sql`:
> единый предикат `private.case_visible(lawyer_id, responsible_id)` (его зовут политики
> cases И `can_see_case` → documents/tasks/payments/comments/storage/лог дела
> наследуют автоматически); `can_see_client`; `payroll_user_visible` (ledger/transactions/
> allocations + 3 RPC). Хелперы `can_see_all_cases`/`scope_is_all`/`current_user_department`/
> `payroll_see_all`. clients/activity_log переписаны; журнал юзеров — по `manage_users`.
> **⚠ Находка адвер-аудита (HIGH, исправлена):** транзишн-правило `department_id IS NULL`
> в `scope_is_all` обязано быть гейтнуто ролью (`admin`/`office_manager`) — иначе admin,
> выдав юристу право `view_all_cases`, эскалировал бы его до видимости всей компании
> (у не-owner `department_id` по умолчанию NULL). Регресс-тесты на эскалацию добавлены.
> Прогон: db reset чистый, tsc/lint/build, unit 49/49, integration 36/36, `/review`
> (critical-pass: clean), security-аудит = 6-агентный адвер-воркфлоу (нашёл+исправил
> HIGH). Пуш/`db push` на прод НЕ делались — ждут «ок».

## Этап 3 — UI: подразделения, команда, доступы

**Цель:** owner управляет структурой из интерфейса.

Задачи:
- `/settings/departments` (owner): список, создание, переименование, деактивация.
- Карточка подразделения: команда (имя, должность, роль), owner назначает
  `department_id` / `position` / `visibility_scope` сотрудникам.
- Управление пользователями: новые поля в форме (owner/admin по правам §4).
- Фильтр «Подразделение» в списках дел и отчётах (для тех, кто видит >1 подразделения).
- Мобильная адаптация по конвенциям проекта (`*-list-mobile.tsx`, нижняя навигация).
- UI-тексты — русский/украинский через словари i18n (`src/lib/i18n/messages`).

DoD: owner может завести подразделение, перекинуть сотрудника, сменить scope —
и видимость меняется (ручная проверка + `/qa` по флоу). Доки: CLAUDE.md §8.

> **✅ Закрыт 2026-06-10.** UI-этап (RLS уже был в Этапах 1–2). Что сделано:
> - **`/settings/departments`** (owner): создание/переименование/(де)активация
>   подразделений + плитка в хабе настроек; карточка каждого подразделения с
>   командой и редактором назначения (`UserAssignmentEditor`): department + scope
>   (owner) + position (любой `manage_users`). Те же поля — в `/settings/users`
>   (колонка «Подразделение») и в форме создания пользователя.
> - **Фильтр «Подразделение»** в `/cases` (обычный список — `.or(lawyer_id.in…,
>   responsible_id.in…)`; поиск — RPC `search_case_ids` + `p_department_id`) и в
>   `/reports/payroll` (пост-фильтр членов). Показывается только тем, кто видит >1
>   (хелпер `canSeeAllCases`, зеркало SQL `can_see_all_cases`).
> - **2 миграции** (аддитивные): `20260610120000` — allowlist `activity_log`
>   + `department_*`/`user_department_changed` + entity_type `department`
>   (owner-gate; ВЕСЬ прежний allowlist сохранён — нет риска 23514);
>   `20260610130000` — `search_case_ids` + `p_department_id` (SECURITY INVOKER,
>   OR по подразделению юриста/эксперта).
> - Двуязычные тексты (ru/uk: словарь `departments` + enum `visibilityScope`).
> - **Прогон:** db reset чистый, tsc/lint/build зелёные, unit 49/49, integration
>   37/37 (+1: фильтр `search_case_ids` по подразделению). Ревью —
>   адверсариальный проход (Windows: gstack-bash не запускается): 0 CRITICAL/HIGH,
>   2 LOW = задокументированное переходное правило `department_id IS NULL ⇒ видит
>   всё` (решение Этапа 2, не новая эскалация). owner-gating подтверждён и на
>   service_role-пути `createUserAction`.
> - **owner-only** для department/scope: код (createUserAction service_role-путь +
>   assignUserDepartmentAction) + БД-гард `users_guard_visibility_fields` +
>   client-гейт. position — любой `manage_users`.
> - Пуш на прод (git + `db push`) НЕ делался — ждёт «ок». Хеш этапа — follow-up.

## Этап 4 — ЗП-режимы

**Цель:** фикс / фикс+% / % на сотруднике, права руководителя.

Задачи:
- Миграция: `users.salary_mode` (`percent|fixed|fixed_percent`, default `percent`),
  `users.salary_fixed_amount numeric null check (>= 0)`.
- Гард прав (по образцу `cases_guard_rate_overrides`): менять salary-поля может owner,
  либо admin для сотрудников своего подразделения.
- UI: блок «Зарплата» в карточке сотрудника (режим, фикс, существующие ставки/override).
- Отчёт `/reports/payroll`: фикс-составляющая (за месяц) + процентная часть;
  `payroll_by_specialist` / `case_payroll` — учесть режим (у `fixed` процентная
  часть = 0).
- `payroll_ledger` — продолжает фиксировать процентные начисления; фикс в леджер v1
  НЕ пишем (только отчёт), отметить в UI.

DoD: расчёты в отчёте корректны для всех 3 режимов (unit-тесты на расчёт);
`/cso` (права) + `/review`. Доки: CLAUDE.md §5 (users, payroll), §7-4.

> **✅ Закрыт 2026-06-10.** Миграция `20260610140000_user_salary_modes.sql`:
> `users.salary_mode` (percent|fixed|fixed_percent, default percent) +
> `salary_fixed_amount numeric(14,2)` с check-консистентностью (percent → NULL;
> fixed/fixed_percent → ≥0 not null). **Права:** `private.can_manage_user_salary`
> (owner — всем; admin — сотруднику своего подразделения, НЕ себе, роли
> office_manager/lawyer/expert; admin без подразделения — никому) + БД-гард
> `users_guard_salary_fields` (по образцу visibility/rate-override; service_role-путь
> пропускает — страж в коде `updateUserSalaryAction`). **Приватность:** `salary_*`
> защищены column-level привилегиями (revoke табличного SELECT + grant безопасного
> списка users), читаются только через SECURITY DEFINER-RPC под `payroll_user_visible`.
> **Расчёт:** `case_payroll` (переведён INVOKER→DEFINER + явный `case_visible`),
> `payroll_by_specialist`, `payroll_employee_summary` (+ колонки `fixed`/`salary_mode`,
> в список попадают окладники без дел), `payroll_employee_cases` — у режима `fixed`
> процент = 0; `fixed_percent` = оклад + %. Оклад — справочно за месяц, в balance
> «К выплате» и выплаты НЕ входит. **Леджер** не трогали (остаётся процентным; в
> текущем UI не рендерится — `CaseLedgerBlock`/`payroll_payout_by_specialist`).
> **UI:** редактор «Зарплата» (`UserSalaryEditor`) в `/settings/users`; колонка
> «Оклад (мес.)» + сноска в `/reports/payroll`; блок оклада на карточке
> `/reports/payroll/[userId]`. **i18n** ru/uk: enum `salaryMode`+hint, `users.salary`,
> `payroll.report.col/totalFixedMonth`+`fixedNote`, `payroll.employee.salary*`.
> **allowlist activity_log:** + `user_salary_changed` (мёрдж всего прежнего, база
> 20260610120000 — гоча 23514 не задета). **Дашборд:** `computePersonalEarnings` и
> `getDashboardAnalytics` тоже учитывают режим (множество окладников
> `getFixedSalaryUserIds` через manage_user_salaries) — иначе сотрудник на окладе
> видел бы ненулевое «Начислено мне» (находка адвер-ревью HIGH, исправлена); чистые
> агрегаторы вынесены в `dashboard/compute.ts` под юнит-тест. **Прогон:** db reset
> чистый, tsc/lint/build зелёные, **unit 54/54** (+5: расчёт режимов в
> computePersonalEarnings), **integration 45/45** (+8: 3 режима расчёта, гард своего/
> чужого/NULL-подразделения, приватность колонок, manage_user_salaries). Ревью —
> 6-агентный адверсариальный проход (gstack `/review`/`/cso` на Windows не
> запускаются, как в Этапах 2–3): нашёл+исправил 1 HIGH (дашборд), приватность/гард/
> отчётные функции/allowlist подтверждены чистыми. Пуш на прод (git + `db push`) НЕ
> делался — ждёт «ок». Хеш коммита — follow-up в статус-таблице.

## Этап 5 — Акты как платёжные документы

**Цель:** цикл «сгенерирован → выдан → оплачен (скан + сумма) → платёж → ЗП».
**Образец документа:** `docs/samples/rahunok-akt-sample.xlsx` («Рахунок-Акт № 40»).

Задачи:
- **Предпосылки для генерации:**
  - реквизиты компании-исполнителя в настройках (owner): назва, ЄДРПОУ, адреса,
    телефон, IBAN + банк/МФО, строки налогового статуса («Не є платником ПДВ»,
    «Є платником єдиного податку, 3 група») — таблица `org_requisites` или
    JSON-настройка; решить на этапе;
  - `clients.tax_id text null` — РНОКПП/ЄДРПОУ заказчика (+ поле в форме клиента);
  - утилита «сумма прописью» на украинском («Дев'ятнадцять тисяч гривень 00 копійок»).
- Миграция: `case_acts` (`id`, `case_id not null`, `number int not null` — сквозная
  нумерация (sequence), `amount numeric not null check (> 0)`,
  `confirmed_amount numeric null` — прописанная при подтверждении сумма,
  `completion text null check (completion in ('full','partial'))`,
  `status text not null default 'issued' check (status in ('issued','paid'))`,
  `issued_at date not null`, `paid_at date null`,
  `scan_document_id uuid null references documents(id)` — подписанный скан,
  `created_by`, `created_at`).
- **Генерация печатной формы** по образцу: print-friendly страница → PDF через печать
  браузера ИЛИ exceljs-шаблон XLSX — выбрать на этапе (критерий: 1:1 с образцом,
  без тяжёлых зависимостей). Номер и дата — автоматически, шапка/подвал из реквизитов.
- RLS: видимость — наследование от дела (`can_see_case`); создание — expert своего
  дела + staff с доступом к делу; подтверждение оплаты — lawyer своего дела +
  admin/owner (создаёт payment → правка/удаление самих payments остаётся owner/admin).
- Подтверждение «оплачен»: форма «скан (обязателен) + оплаченная сумма + дата» →
  автосоздание `payments` (сумма = confirmed_amount, связь `payments.act_id`) →
  существующие триггеры paid_total/debt/ЗП работают; `completion` вычисляется
  (сумма оплат по актам ≥ `contract_sum` → `full`, иначе `partial`), staff может
  переопределить вручную.
- Блок закрытия дела: переход в `closed` запрещён, если нет акта `full`+`paid`
  (БД-триггер + понятная ошибка в UI); актов нет вообще — мягкое предупреждение
  (текущее поведение сохраняется).
- Право эксперта создавать задачи по своим делам (политика tasks + UI).
- Карточка дела: секция «Акты» (сгенерировать, скачать/печать, подтвердить оплату;
  статусные бейджи issued/paid + full/partial), оптимистичный UI по образцу платежей.
- `activity_log`: новые действия по актам (⚠ allowlist!).

DoD: e2e-флоу «акт сгенерирован (номер/реквизиты/сумма прописью корректны) → скан +
сумма → платёж появился → долг уменьшился → ЗП выросла → дело закрывается только при
full+paid» покрыт integration-тестом; печатная форма сверена с образцом глазами;
`/cso` + `/review`.
Доки: CLAUDE.md §5 (case_acts, clients.tax_id, org_requisites), §6 (закрытие),
§7-6, §7-8.

> **✅ Закрыт 2026-06-10.** Развилки с пользователем: печать — **exceljs→XLSX** по
> образцу; реквизиты — отдельная таблица **`org_requisites`** (owner, `/settings/requisites`,
> засеяна ОЛІМП); закрытие дела — **мягкое предупреждение** (жёсткий блок full+paid НЕ
> включён до подтверждения клиентом → Открытый вопрос №1 переведён в «решено: пока soft»).
> `tax_id` НЕ вводили — переиспользован существующий `clients.inn` (РНОКПП/ЄДРПОУ).
> «Право эксперта на задачи» уже работало (RLS `can_write_case`) — только правка §7-6.
> **Миграции:** `20260610150000_org_requisites` (single-row, RLS owner-update + seed),
> `20260610160000_case_acts` (таблица + sequence номера + `payments.act_id` unique +
> RLS [SELECT от дела, INSERT Експерт/staff, DELETE только issued, UPDATE НЕТ] +
> DEFINER `confirm_act_paid` [скан→documents + платёж + paid + пересчёт completion +
> журнал, атомарно, FOR UPDATE] + `set_act_completion` [staff] +
> `recompute_case_act_completions` [единый источник completion] + триггер реверта при
> удалении платежа), `20260610170000_activity_log_act_actions` (+ act_created/act_paid,
> мёрдж всего allowlist, база 20260610140000 — 23514 не задета).
> **Код:** `lib/acts` (queries/actions/xlsx/amount-in-words), `lib/org`, route
> `/cases/:id/acts/:actId/xlsx`, `components/acts/*`, секция «Акты» на карточке дела,
> `/settings/requisites` + плитка, i18n ru/uk (`acts`, `requisites`, enum
> actStatus/actCompletion). `tax_id` форма клиента уже была (поле `inn`).
> **Прогон:** db reset чистый, tsc/lint/build зелёные, **unit 68/68** (+10 сумма
> прописью, +4 XLSX), **integration 52/52** (+7: создание Експерт/запрет lawyer,
> запрет подтверждения Експертом, подтверждение lawyer→full+платёж+долг 0,
> повторное подтверждение, partial, реверт акта при удалении платежа).
> Ревью — 2 адверсариальных агента (security/RLS + корректность; gstack `/review`/`/cso`
> на Windows не идут): эксплуатируемых CRIT/HIGH нет; исправлены 4 MEDIUM —
> (1) рассинхрон гейта подтверждения (action/UI выровнены на роль owner/admin, как RPC),
> (2) осиротевший documents-ряд при откате (создание скана перенесено ВНУТРЬ атомарной
> RPC — откат чистит лишь storage), (3) стейл completion соседних актов при удалении
> платежа (recompute из реверта), (4) FOR UPDATE в confirm + сверка case_id в xlsx-route.
> Печать «Усього/До оплати» = `amount` (сумма счёта) — by design (счёт-инвойс).
> Пуш на прод (git + `db push`) НЕ делался — ждёт «ок». Хеш этапа — follow-up в таблице.

## Этап 6 — Отпуска / отсутствия

Задачи:
- Миграция: `absences` (`id`, `user_id not null`, `kind text check (in
  'vacation','sick','other')`, `starts_on date not null`, `ends_on date not null
  check (ends_on >= starts_on)`, `note text null`, `created_by`, `created_at`).
- RLS: читать — owner все, admin/office_manager — своё подразделение, сотрудник — себя;
  писать — owner, admin (своё подразделение), сам сотрудник; удалять — owner/admin.
- UI: блок в карточке сотрудника + события в общем календаре (отличимы от
  заседаний/дедлайнов цветом/иконкой).

DoD: tsc/lint/build, базовые тесты RLS, `/review`. Доки: CLAUDE.md §5, §8.

> **✅ Закрыт 2026-06-10.** Миграция `20260610180000_absences.sql`: таблица `absences`
> (`user_id` cascade, `kind` vacation|sick|other, `starts_on`/`ends_on` + CHECK
> `ends_on ≥ starts_on`, `note` ≤500, `created_by` restrict). **Доступ — РОЛЕВОЙ, не по
> cap** (в отличие от `payroll_user_visible`): 2 предиката `private.absence_user_visible`
> (чтение: сам · owner · admin/office_manager своего подразделения / scope='all' / NULL)
> и `private.absence_can_write` (запись: сам · owner · **admin** подразделения —
> office_manager НЕ пишет). RLS: SELECT/INSERT (`created_by=active_uid` + can_write)/
> DELETE (can_write ИЛИ автор); **UPDATE-политики НЕТ** (правка = удалить+создать).
> `activity_log` для отпусков не ведём (не «по делам» → гоча 23514 не задета).
> **Код:** `lib/absences` (queries `listAbsencesByUser`/`listAbsencesInRange`; actions
> create/delete; `access.ts` — TS-зеркало предикатов под юнит-тест), типы `Absence`/
> `AbsenceKind`/`AbsenceWithUser`. **UI:** блок «Отпуска и отсутствия» (`AbsencesBlock` +
> форма + удаление) на `/reports/payroll/[userId]`; в общем календаре — violet-маркеры
> (токен `--absence`, раскрытие диапазона по дням сетки) + пункт легенды + список в
> панели дня. i18n ru/uk (`absences`, enum `absenceKind`). DESIGN.md — токен `--absence`.
> **Развилки (из плана сессии, согласованы):** календарь — пер-дневные маркеры (не
> спан-полосы); блок живёт на карточке `/reports/payroll/[userId]`; office_manager только
> читает; сотрудник может удалить свой отпуск; без `activity_log`.
> **Прогон:** db reset чистый, tsc/lint/build зелёные, **unit 81/81** (+13 предикаты
> доступа), **integration 72/72** (+20: матрица видимости сам/admin-своё/admin-чужое/
> office_manager-чтение/scope=all/NULL/owner; INSERT сам/admin/owner + запреты чужому/
> office_manager/спуф created_by; DELETE admin-своё/автор + запреты office_manager/чужой
> admin; CHECK range/kind). Починена коллизия email в `system.test.ts` (фикстуры теперь
> содержат `owner` — department-CRUD тест отрефакторен на него). Ревью — 6-агентный
> адверсариальный workflow (security/RLS + корректность + полнота-vs-спека → верификация
> каждой находки; gstack `/review`/`/cso` на Windows не идут): эксплуатируемых CRIT/HIGH/
> MED нет; 2 LOW исправлены — комментарий о намеренном silent-fail `deleteAbsenceAction`
> (зеркало `deleteActAction`) + `--absence` внесён в таблицу семантики DESIGN.md.
> Пуш на прод (git + `db push`) НЕ делался — ждёт «ок». Хеш этапа — follow-up в таблице.

## Этап 7 — Касса и сальдо-отчёт

**Образец:** `docs/samples/oborotka-olimp-sample.xls` (3 листа-кассы: по дню — остаток
на начало, до 10 строк прихода, до 10 строк расхода, остаток на конец; лист Total —
остатки по всем кассам на конец каждого дня + сумма).

Задачи:
- Миграции: `cash_accounts` (`id`, `name`, `kind text check (in 'card','bank','cash')`,
  `opening_balance numeric not null default 0`, `opening_date date not null`,
  `is_active bool default true`); `cash_entries` (`id`, `account_id not null`,
  `entry_date date not null`, `direction text check (in 'in','out')`,
  `amount numeric check (> 0)`, `description text not null`, `case_id uuid null`,
  `payment_id uuid null`, `created_by`, `created_at`).
- Право: `users.can_manage_cash boolean default false` — выдаёт только owner
  (owner всегда имеет доступ).
- RLS: всё по `can_manage_cash` или owner.
- Автоприход: при внесении платежа по делу — выбор счёта (маппинг `payments.method` →
  счёт по умолчанию), создаётся `cash_entries(in)` со ссылкой на платёж; правка/откат
  платежа корректно отражается в кассе.
- Сидинг: 3 счёта (Карта, Рахунок, Готівка).
- UI `/reports/cash`: вкладки счетов + Total; таблица по дням с разворотом
  (остаток на начало → приходы → расходы → остаток на конец); итоги месяца;
  ручное добавление операции (приход/расход, счёт, дата, сумма, описание);
  моноширинные цифры, минус — красным.
- Сальдо считается накопительно от `opening_balance`/`opening_date` (выводить
  предупреждение, если есть операции раньше opening_date).

DoD: суммы сходятся с контрольным примером из XLS (юнит-тест на расчёт сальдо);
`/cso` (новый модуль прав) + `/review`. Доки: CLAUDE.md §5, §8 (Phase 2 — касса
реализована).

> **✅ Закрыт 2026-06-11 — ПОСЛЕДНИЙ этап цикла v2.** Миграция
> `20260610190000_cash_register.sql` (аддитивная): таблицы `cash_accounts`
> (name/kind card|bank|cash/opening_balance/opening_date/is_active/is_default,
> partial-unique ≤1 default) и `cash_entries` (account_id RESTRICT, entry_date,
> direction in|out, amount>0, description ≤300, case_id SET NULL, payment_id CASCADE
> unique, created_by RESTRICT). **Развилки (согласованы, подтверждены адвер-ревью):**
> (1) `can_manage_cash` — **12-е право в `perm_overrides`** (НЕ отдельная boolean-колонка,
> как в букве плана): дефолт owner, **выдаёт только owner** (`can_grant_cap` owner-only
> ветка), так касса автоматически в редакторе прав/`private.can`/TS-зеркале; (2)
> «выбор счёта» = **маппинг `payments.method`→счёт** (триггер `cash_sync_on_payment`,
> SECURITY DEFINER; card/bank/cash, `'act'`→bank; фолбэк — дефолтный счёт; нет касс →
> пропуск без падения), отдельный picker НЕ добавляли — ревью подтвердил, что это
> верное чтение «(маппинг payments.method → счёт по умолчанию)». Удаление платежа →
> FK cascade снимает приход; правка → пересоздаёт. **Сальдо** — чистый TS
> (`lib/cash/saldo.ts`), накопительно от opening_balance, юнит-тест по образцу ОЛІМП
> (`docs/samples/oborotka-olimp-sample.xls`). **UI** `/reports/cash`: управление
> счетами + вкладки счетов/Total + разворот по дням (остаток на начало→приход→расход→
> на конец) + итоги месяца + журнал операций (удаление ручных) + форма ручной операции;
> моноширинные цифры, минус красным; пункт «Касса» в навигации (cap-gated, репрпоз
> stub `finance`). **Сидинг** 3 счёта в `scripts/seed.ts` + право office-менеджеру.
> **Адвер-ревью** (15-агентный workflow, gstack на Windows не идёт): из 35 находок
> подтверждено 5, исправлены: **HIGH** — `cash_sync_on_payment` резолвил счёт ПОСЛЕ
> удаления старой строки → правка платежа в метод без счёта теряла приход; теперь
> резолв ПЕРВЫМ, удаляем только если есть куда переложить; **MEDIUM** — гард
> неизменности `created_by`/`created_at` кассы (BEFORE UPDATE триггер, пин к old —
> НЕ `=active_uid`, иначе ломалась бы правка чужого счёта другим cash-manager);
> **MEDIUM** — добавлен integration-тест акт→касса (`confirm_act_paid`→приход на bank).
> Остальное — отклонено (race на дефолтном счёте безопасен из-за partial-unique +
> 23505→friendly; picker не нужен). **Прогон:** db reset чистый, tsc/lint/build
> зелёные, **unit 93/93** (+12 сальдо ОЛІМП), **integration 85/85** (+13 касса: видимость
> по cap, автоприход bank/act, cascade при удалении, ручные операции, запрет спуфа
> payment_id, owner-only грант). Пуш на прод (git + `db push`) НЕ делался — ждёт «ок».
> Хеш этапа — follow-up в статус-таблице.

---

## Бэкап и откат

**Локальная БД** — одноразовая: пересоздаётся из миграций + seed
(`npx supabase db reset`), бэкапить нечего.

**Прод (Supabase, проект `fmzevqyquljecmsiqsoj`)** — единственное хранилище реальных
данных. Правила:
- **Перед КАЖДЫМ `npx supabase db push` на прод — обязательный дамп** (Docker должен
  быть запущен; CLI спросит пароль БД — он у пользователя, из Supabase Dashboard →
  Settings → Database):
  ```bash
  mkdir -p backups
  npx supabase db dump --linked -f backups/prod-schema-YYYY-MM-DD.sql
  npx supabase db dump --linked --data-only --use-copy -f backups/prod-data-YYYY-MM-DD.sql
  ```
  Проверить, что оба файла не пустые. `backups/` в `.gitignore` — НЕ коммитить
  (персональные данные клиентов).
- **Откат данных/схемы:** накатить дамп через psql на чистую БД / связаться с
  поддержкой Supabase (на платном плане есть авто-бэкапы в Dashboard → Database →
  Backups). Для ошибки в миграции предпочтительнее **down-миграция** (новая миграция,
  отменяющая изменение), а не восстановление дампа — дамп теряет данные, внесённые
  после него.
- **Файлы документов (Storage)** в SQL-дамп НЕ входят. Наши миграции файлы не трогают,
  риск низкий; при операциях со Storage — отдельная копия бакета.
- **Код:** откат через git (прод деплоится с master) + у Vercel хранятся предыдущие
  деплои (Instant Rollback в дашборде).
- Все миграции цикла v2 — аддитивные (новые таблицы/колонки/политики), существующие
  данные они не разрушают; дамп — страховка, а не план.

## Подводные камни (для всех этапов)

- **RLS:** запросы от лица пользователя — только через клиент с его сессией;
  `service_role` — никогда в пользовательских маршрутах (CLAUDE.md §2).
- **Миграции и `activity_log`:** любая миграция, пересоздающая
  `activity_log_action_check` / `log_activity`, обязана ОБЪЕДИНЯТЬ весь прежний
  allowlist действий (база — `20260601100000`), иначе `db push` на прод упадёт с 23514,
  хотя локально всё ок.
- **Next.js 16 / React 19 / Tailwind 4:** перед Server Actions/middleware читать
  локальные доки `node_modules/next/dist/docs/`; конфиг Tailwind — в CSS через
  `@theme` (`globals.css`), `tailwind.config.ts` НЕ создавать.
- **Дизайн:** перед вёрсткой читать `DESIGN.md`; цвета только из токенов
  (активная тема teal через `data-theme`).
- **Тесты:** конвенции в `tests/README.md`; integration самодостаточны
  (IT-namespace + cleanup).
- **Коммиты:** один блок на этап, не дробить; пуш master = автодеплой прода Vercel —
  только по «ок» пользователя.
