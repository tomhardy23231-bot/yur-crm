# PROGRESS — Юр CRM

> **Назначение.** Этот файл — единственный источник правды о ходе разработки между
> сессиями. В начале каждой новой сессии первым делом читай его (последнюю запись)
> сразу после `CLAUDE.md`. По команде пользователя «завершаем сессию» — обязательно
> добавь сюда новый handoff-блок.

---

## Текущее состояние (актуально на 2026-05-26)

- **Шаг:** 0 — Инициализация и gstack
- **Подшаг:** план Шага 0 согласован, реализация ещё не начата
- **Последний коммит:** репозиторий ещё не инициализирован
- **Следующее действие:** дождаться явного «ок» от пользователя на план Шага 0,
  затем выполнить пункты 1–8 плана (см. чат / kickoff-prompt.md)

### Открытые решения
- **Git remote:** отложен. Пока коммитим только локально, remote (GitHub/GitLab) подключим
  по запросу пользователя.
- **Стек:** Next.js App Router + TypeScript + Supabase + Tailwind + shadcn/ui + npm
  (не bun) — зафиксировано CLAUDE.md §2/§3.
- **Docker Desktop** установлен на машине пользователя — `supabase start` будет работать
  локально (запустим в Шаге 1, когда появятся миграции).

---

## Регламент сессий

### Завершение сессии
Когда пользователь пишет «завершаем сессию» (или эквивалент: «заканчиваем», «на сегодня
всё», «session end»), агент обязан **перед прощанием**:

1. Дописать в раздел [Лог сессий](#лог-сессий) **новый блок** по шаблону ниже.
2. Обновить раздел [Текущее состояние](#текущее-состояние-актуально-на-2026-05-26)
   (заменить целиком — это снимок «на сейчас»).
3. Если есть незакоммиченные изменения — спросить, коммитить ли. **Не коммитить
   автоматически.**
4. Подтвердить пользователю одной строкой:
   `Сессия зафиксирована в docs/PROGRESS.md — в новой сессии начни с этого файла.`

### Начало новой сессии
Первое действие агента в новой сессии:
1. Прочитать `CLAUDE.md` (особенно §7 бизнес-правила и §11 дизайн).
2. Прочитать **последнюю запись** в [Лог сессий](#лог-сессий) этого файла.
3. Прочитать раздел [Текущее состояние](#текущее-состояние-актуально-на-2026-05-26).
4. Только после этого предлагать конкретное действие.

### Шаблон записи в лог
```
## Сессия YYYY-MM-DD

**Шаг(и):** N — короткое название
**Длительность:** ~X часов
**Модель:** Claude Opus 4.7 (или другая)

### Сделано
- Конкретный пункт 1 (файлы: path/to/file.ts)
- Конкретный пункт 2 (миграции: 20260526_...sql)
- Конкретный пункт 3

### Решения и почему
- Решение → причина. Если расходится с CLAUDE.md — пометить «отклонение, согласовано».

### Незакрытые вопросы / TODO
- [ ] Что осталось внутри текущего шага
- [ ] Что заметили попутно для следующих шагов

### Handoff для следующей сессии
- **Стартовать с:** конкретная задача (один абзац).
- **Файлы открыть в первую очередь:** path/a, path/b
- **Команды для проверки текущего состояния:** `npm run dev`, `npx supabase status`, ...
- **Подводные камни:** что может сломаться или сбить с толку.

### Коммиты
- `<sha>` `<subject>` (если репозиторий уже инициализирован)
```

---

## Лог сессий

<!-- Новые записи добавляются СВЕРХУ (новейшая первой). Append-only — историю не переписывать. -->

## Сессия 2026-05-26 (стартовая)

**Шаг(и):** 0 — инициализация (план)
**Длительность:** короткая (планирование)
**Модель:** Claude Opus 4.7 (1M context)

### Сделано
- Прочитан `CLAUDE.md` и `kickoff-prompt.md` целиком.
- Согласован формат сессионных handoff-ов: правило зафиксировано в memory агента
  и в этом файле.
- Создана директория `docs/` и файл `docs/PROGRESS.md` (этот).
- Согласован план Шага 0 (8 пунктов: Next.js init, tsconfig strict, структура папок,
  `supabase init`, `.env.example`, shadcn init, обновление §3 CLAUDE.md, `git init`).

### Решения и почему
- **npm, не bun** — клиент явно подтвердил; CLAUDE.md §3 написан под npm.
- **Git remote отложен** — подключим по запросу.
- **`supabase start` НЕ запускаем в Шаге 0** — Docker-контейнеры поднимем в Шаге 1,
  когда будут миграции. У пользователя Docker Desktop установлен.
- **shadcn/ui — только init, без компонентов** — конкретные компоненты ставим под
  реальные экраны в Шагах 4+. Тема будет переопределена в Шаге 3
  через `/design-consultation`, поэтому стартовая Slate-палитра — временная.

### Незакрытые вопросы / TODO
- [ ] Запустить пункты 1–8 плана Шага 0 после явного «ок» от пользователя.

### Handoff для следующей сессии
- **Стартовать с:** проверить, что пользователь дал «ок» на план Шага 0 (см. чат).
  Если да — выполнять пункты по порядку, обновляя todo-лист и фиксируя каждый шаг.
  Если в новой сессии — спросить «продолжаем Шаг 0?» и зачитать план кратко.
- **Файлы открыть в первую очередь:** `CLAUDE.md`, `kickoff-prompt.md`, этот файл.
- **Команды для проверки текущего состояния:** `ls "c:/Users/HP/Desktop/Юр система"`
  (должно быть пусто кроме `CLAUDE.md`, `kickoff-prompt.md`, `docs/`).
- **Подводные камни:** не запускать `npx create-next-app` без подтверждения — создаёт
  множество файлов, откатывать вручную. Не использовать `service_role` для пользовательских
  запросов (CLAUDE.md §2). Дизайн — НЕ Inter/Roboto/Arial, НЕ фиолетовые градиенты (§11).

### Коммиты
— (репозиторий ещё не инициализирован)
